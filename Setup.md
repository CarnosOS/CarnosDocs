Setting up CarnosOS
==============

#1: Setting up the registry system

To set up the registry system, you will have to make sure that the config.php file at the root of the operating system is setup with the right settings.
Make sure that the mysql host, username and password are correct. Then install the OS by going to the installer page.
